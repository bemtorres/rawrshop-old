<?php

use Illuminate\Support\Facades\Route;

// Route::middleware('auth.mix')->prefix('web')->namespace('Admin')->group( function () {
// });

// Route::get('template/{id}','Web\WebController@template')->name('template');
